<h1><%= t('order_detail.edit') %></h1>

<%= render 'form', order_detail: @order_detail %>

<%= link_to t('links.show'), @order_detail, class:"btn btn-primary"  %>
<%= link_to t('links.back'), order_details_path, class:"btn btn-outline-primary"  %>
