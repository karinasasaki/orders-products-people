<span style="color:green" ><%= notice %></span>

<p>
  <strong><%= Person.human_attribute_name(:id) %>: </strong>
  <%= @person.id %>
  <br>
  <strong><%= Person.human_attribute_name(:name) %>: </strong>
  <%= @person.name %>
  <br>
  <strong><%= Person.human_attribute_name(:created_at) %>: </strong>
  <%= datahora_br(@person.created_at) %>
  <br>
  <strong><%= Person.human_attribute_name(:updated_at) %>: </strong>
  <%= datahora_br(@person.updated_at) %>
</p>

<hr class="my-4">

<div class="row">
  <div class="col">
    <p>
      <strong><%= Order.model_name.human(count: 2) %>: </strong>
      <br>

      
      <%@results.each do |order| %>
      <%puts '=========RESULTS========='%>
      <%puts order.name%>
       <b>Código da compra:</b> <%= order.id %>
       <b>Nome do pedido:</b> <%= order.name %>
       <b>Criado em:</b> <%= order.created_at.strftime("%d/%m/%Y %H:%M") %>
       <b>Produtos comprados:</b> <%= order.products.pluck(:name).join(", ") %>
       <b>Valor total:</b> R$ <%= order.total_value %>
      <%end%>
    </p>
  </div>
  <div class="col">
    <%= form_tag(person_path, method: :get) do %>
      <h2>Pesquisar</h2>
      <div class="row">
        <div class="col-10">
          <%= search_field_tag :search, params[:search], placeholder: "Type", class:"form-control" %>
        </div>
        <div class="col-1">
          <%= button_tag "Bus por produto", :class => 'btn btn-info glyphicon glyphicon-search', :name => nil%>
        </div>
      </div>
    <% end %>
    
    
  </div>
  
</div>

<br>

<%= link_to t('links.edit'), edit_person_path(@person), class:"btn btn-primary"  %>
<%= link_to t('links.back'), people_path, class:"btn btn-outline-primary"   %>
